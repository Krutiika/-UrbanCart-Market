server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      default-filters:
        - "AddRequestHeader=X-Correlation-Id, ${T(java.util.UUID).randomUUID().toString()}"
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
